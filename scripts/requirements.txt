# Neural audio embeddings (similarity search)
transformers>=4.40.0

# Video download
yt-dlp>=2023.12.30

# Source separation (torchcodec required by torchaudio 2.6+ for audio saving)
demucs>=4.0.0
torchcodec>=0.1.0

# Beat tracking (madmom requires Cython to build)
Cython>=3.0.0
madmom>=0.16.1

# Pitch detection / transcription
basic-pitch>=0.3.0

# Audio features
librosa>=0.10.0
soundfile>=0.12.1

# Object storage (R2 is S3-compatible)
boto3>=1.34.0

# Web scraping (smalls_scraper)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Image processing (used by visual_detector CLIP pass)
Pillow>=10.0.0

# Utilities
numpy>=1.24.0,<2
scipy>=1.11.0
tqdm>=4.66.0
