# Neural audio embeddings (similarity search)
transformers>=4.40.0

# Video download
yt-dlp>=2023.12.30

# Source separation (pin to 4.0.0 — 4.0.1 added torchcodec which has no arm64 Linux wheels)
demucs==4.0.0

# Pitch detection / MIDI transcription
basic-pitch>=0.3.0
pretty_midi>=0.2.10

# Object storage (R2 is S3-compatible)
boto3>=1.34.0

# Web scraping (smalls_scraper)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Image processing (used by visual_detector CLIP pass)
Pillow>=10.0.0

# Utilities
numpy>=1.24.0,<2
scipy>=1.11.0
tqdm>=4.66.0
soundfile>=0.12.1
